# 數學學習資源中心 — 工具使用說明
# Maths Learning Hub — User Guide

> **適用對象 / For:** HKDSE 數學（必修）及數學（模組二 M2）學生
> **版本 / Version:** 2025–26 學年
> **網站 / Website:** [https://kingtmc314.github.io/maths-hub/](https://kingtmc314.github.io/maths-hub/)

---

## 目錄 / Table of Contents

1. [平台概覽 Platform Overview](#1-平台概覽-platform-overview)
2. [三角計算器（2D & 3D）](#2-三角計算器2d--3d--trigonometry-calculator-2d--3d)
3. [排列、組合及概率](#3-排列組合及概率--permutation-combination--probability)
4. [線性代數實驗室](#4-線性代數實驗室--linear-algebra-lab)
5. [級數與歸納法計算器](#5-級數與歸納法計算器--series-and-induction-calculator)
6. [微分與曲線描繪](#6-微分與曲線描繪--differentiation-and-curve-sketching)
7. [積分：面積與體積](#7-積分面積與體積--integration-area-and-volume)
8. [不定積分與定積分](#8-不定積分與定積分--indefinite--definite-integration)
9. [常見問題 FAQ](#9-常見問題-faq)

---

## 1. 平台概覽 Platform Overview

**數學學習資源中心**是一個整合 HKDSE 數學必修及 M2 互動工具的學習入口網站。所有工具均以精確值（分數、根號、π）輸出答案，並以 LaTeX 格式顯示完整解題步驟，支援中英文介面切換。

**The Maths Learning Hub** is a curated portal integrating interactive tools for HKDSE Core Mathematics and Module 2 (M2). All tools output exact-value answers (fractions, surds, π) with full LaTeX step-by-step working, and support bilingual (Chinese/English) interface switching.

| 科目分類 | 工具 | 連結 |
|----------|------|------|
| 數學（必修） | 三角計算器（2D & 3D） | [連結](https://kingtmc314.github.io/solid-geometry-calculator/) |
| 數學（必修） | 排列、組合及概率 | [連結](https://kingtmc314.github.io/dse-pc-prob-calculator/) |
| 數學 (M2) — 代數 | 線性代數實驗室 | [連結](https://kingtmc314.github.io/linear-algebra-lab/) |
| 數學 (M2) — 代數 | 級數與歸納法計算器 | [連結](https://seriescalc-mdhgko93.manus.space) |
| 數學 (M2) — 微積分 | 微分與曲線描繪 | [連結](https://kingtmc314.github.io/curve-analyzer/) |
| 數學 (M2) — 微積分 | 積分：面積與體積 | [連結](https://kingtmc314.github.io/integcalc-lab/) |
| 數學 (M2) — 微積分 | 不定積分與定積分 | [連結](https://kingtmc314.github.io/integral-calculator-students/) |

**語言切換 / Language Toggle：** 點擊右上角「中 / EN」按鈕即可在中英文介面之間切換，設定會自動儲存。

---

## 2. 三角計算器（2D & 3D） / Trigonometry Calculator (2D & 3D)

**連結：** https://kingtmc314.github.io/solid-geometry-calculator/

### 功能概覽

本工具涵蓋平面（2D）及立體（3D）三角計算，適用於 HKDSE 數學必修課程中的正弦定理、餘弦定理及立體幾何題型。

### 操作步驟

**第一步：建立底面（Base）**

使用者首先選擇底面的多邊形類型：

| 底面類型 | 說明 |
|----------|------|
| 三角形 | 預設三個頂點（可自訂標籤，如 A/B/C） |
| 正方形 | 四個頂點 |
| 五邊形 | 五個頂點 |
| 六邊形 | 六個頂點 |

- 在「輸入已知數據」欄位中，輸入已知的**邊長**或**角度（°）**。
- 頂點標籤可自訂（預設為 A、B、C）。
- 圖形支援**拖曳頂點**調整位置，並可開啟格線輔助。

**第二步：升成立體（Elevate to 3D）**

完成底面計算後，點擊「升成立體 →」按鈕，進入立體幾何計算模式，輸入立體的高度或其他已知量。

**第三步：查看計算結果**

計算結果分為四個標籤頁：

| 標籤 | 內容 |
|------|------|
| **步驟** | 逐步 LaTeX 解題過程，標明所用定理（正弦 / 餘弦定理） |
| **結果** | 所有邊長與角度的精確值及小數近似值 |
| **四心** | 內心、外心、垂心、重心的坐標及性質 |
| **面積** | 三角形面積計算（含公式） |

**匯出功能：** 點擊右上角「匯出 PDF」按鈕，可將完整解題步驟匯出為 PDF 文件。

### 輸入格式說明

- 邊長輸入任意正數（如 `5`、`3.5`、`√3` 可輸入 `1.732`）。
- 角度以**度（°）**為單位輸入（如 `36.87`）。
- 至少需輸入三個已知量（其中至少一個為邊長）方可求解。

### 適用題型

- 已知三邊求三角（SSS）
- 已知兩邊一夾角求解（SAS）
- 已知兩角一邊求解（AAS / ASA）
- 立體幾何中的角度與距離計算

---

## 3. 排列、組合及概率 / Permutation, Combination & Probability

**連結：** https://kingtmc314.github.io/dse-pc-prob-calculator/

### 功能概覽

本工具以互動故事情境的方式，引導學生學習排列、組合及概率計算，適用於 HKDSE 數學必修課程。

### 操作步驟（五步引導流程）

本工具採用五步引導式介面：

| 步驟 | 操作 |
|------|------|
| **第一步：選情境** | 選擇一個故事背景（辦公室選拔、影畢業相、茶餐廳點菜、家庭聚餐） |
| **第二步：設數量** | 輸入各類別的人數或物品數量 |
| **第三步：選動作** | 選擇計算類型（排列 / 組合 / 概率） |
| **第四步：加條件** | 設定限制條件（如特定人必須入選、相鄰限制等） |
| **第五步：看結果** | 查看逐步計算過程及最終答案 |

### 情境說明

| 情境 | 適用題型 |
|------|----------|
| 👔 辦公室選拔 | 從經理和員工中選出委員會或排隊 |
| 👨‍🏫 影畢業相 | 老師和學生排隊影相（含相鄰 / 不相鄰條件） |
| 🍛 茶餐廳點菜 | 從不同菜式和飲品中選擇（含至少 / 至多條件） |
| 👨 家庭聚餐 | 成人和兒童圍坐或排隊（含圓形排列） |

### 計算類型

- **排列 P(n, r)**：從 n 個物件中取 r 個作有序排列，$P(n,r) = \dfrac{n!}{(n-r)!}$
- **組合 C(n, r)**：從 n 個物件中取 r 個作無序選取，$C(n,r) = \dfrac{n!}{r!(n-r)!}$
- **概率**：計算事件發生的概率，支援條件概率 $P(A|B) = \dfrac{P(A \cap B)}{P(B)}$
- **互斥事件**：$P(A \cup B) = P(A) + P(B)$（當 $A \cap B = \emptyset$）
- **獨立事件**：$P(A \cap B) = P(A) \cdot P(B)$

---

## 4. 線性代數實驗室 / Linear Algebra Lab

**連結：** https://kingtmc314.github.io/linear-algebra-lab/

### 功能概覽

本工具專為 HKDSE M2 線性代數設計，所有答案以精確值（分數、根號）輸出，並附逐步推導過程。支援 2×2 及 3×3 矩陣運算。

### 主要功能模組

#### 矩陣基本運算

| 運算 | 說明 |
|------|------|
| 加法 / 減法 | 同維矩陣相加減 |
| 純量乘法 | 矩陣乘以常數 |
| 矩陣乘法 | 兩矩陣相乘（自動檢查維度相容性） |
| 轉置矩陣 | 求矩陣的轉置 $A^T$ |

#### 行列式與逆矩陣

- **行列式 det(A)**：以展開法逐步計算，顯示完整過程。
- **逆矩陣 A⁻¹**：計算前自動檢查行列式是否為零（奇異矩陣檢查），若矩陣不可逆則顯示提示。
- 逆矩陣以精確分數形式輸出。

#### 特徵值與對角化

本模組是 M2 考試的核心功能：

1. **特徵值 (Eigenvalues)**：解特徵方程 $\det(A - \lambda I) = 0$，顯示完整展開過程。
2. **特徵向量 (Eigenvectors)**：對每個特徵值求對應的特徵向量，以精確值表示。
3. **對角化 (Diagonalisation)**：
   - 建構矩陣 $P$（特徵向量為列）及對角矩陣 $D$（特徵值在對角線）
   - 驗證 $A = PDP^{-1}$
4. **矩陣 n 次方 $A^n$**：
   - 利用 $A^n = PD^nP^{-1}$ 計算
   - 輸入具體 $n$ 值時直接計算精確結果；$n$ 為符號時保留 $D^n$ 形式
   - 極大數值以指數記法表示

#### 線性方程組

- 以高斯消元法（Gaussian Elimination）逐步求解 $Ax = b$。
- 支援唯一解、無解及無限多解（以任意常數 $k$ 表示）三種情況。

#### 2D / 3D 線性變換視覺化

- 輸入 2×2 或 3×3 矩陣後，以互動圖表展示線性變換對向量的幾何效果。
- 可觀察旋轉、縮放、剪切等變換的視覺效果。

### 輸入格式

- 矩陣元素以**分數**（如 `1/2`）或**整數**（如 `3`）輸入。
- 支援負數（如 `-2`）及小數（如 `1.5`）。

---

## 5. 級數與歸納法計算器 / Series and Induction Calculator

**連結：** https://seriescalc-mdhgko93.manus.space

### 功能概覽

本工具分為三個部分，涵蓋 M2 級數的封閉式推導、數學歸納法證明及數值計算驗證。

### 第一部分：封閉式推導（Closed-Form Derivation）

**目的：** 將 $\sum_{r=a}^{n} f(r)$ 化簡為關於 $n$ 的封閉式表達式。

**操作方法：**

| 欄位 | 說明 | 範例 |
|------|------|------|
| 下限 (Lower Limit) | 求和的起始值 | `1`、`0`、常數 |
| 上限 (Upper Limit) | 求和的終止值 | `n`、`2n`、具體數字 |
| 通項 (General Term) | 每一項的表達式 | `r`、`r^2`、`r*(r+1)`、`1/(r*(r+1))` |

計算器會自動利用以下標準公式進行分拆與化簡：

$$\sum_{r=1}^{n} 1 = n, \quad \sum_{r=1}^{n} r = \frac{n(n+1)}{2}, \quad \sum_{r=1}^{n} r^2 = \frac{n(n+1)(2n+1)}{6}, \quad \sum_{r=1}^{n} r^3 = \left[\frac{n(n+1)}{2}\right]^2$$

結果以 LaTeX 精確值顯示，並可在「公式速查」頁面查閱所有標準公式。

### 第二部分：數學歸納法證明（Proof by Mathematical Induction）

**前提：** 須先完成第一部分的封閉式推導。

本部分自動生成完整的**第一數學歸納法**證明，包含：

1. **基礎步驟（Base Case）**：驗證 $n = 1$（或指定下限）時命題成立。
2. **歸納假設（Inductive Hypothesis）**：假設 $n = k$ 時命題成立。
3. **歸納步驟（Inductive Step）**：證明 $n = k+1$ 時命題亦成立，顯示完整代數推導。
4. **結論（Conclusion）**：以標準格式作結。

> **注意：** 上限應為 $n$ 或 $2n$ 以生成歸納法證明。

### 第三部分：數值計算驗證（Numeric Evaluator）

**目的：** 以具體數值驗算封閉式結果。

- 輸入具體的數值下限和上限（如下限 `3`，上限 `10`）。
- 若與第一部分的上下限不同，計算器自動應用**分拆求和恆等式**：$S(a,b) = F(b) - F(a-1)$。
- 若第一部分無封閉式，則直接以逐項求和計算。

---

## 6. 微分與曲線描繪 / Differentiation and Curve Sketching

**連結：** https://kingtmc314.github.io/curve-analyzer/

### 功能概覽

本工具提供函數的完整微分分析，包括圖形繪製、導函數計算、曲線特徵分析及切線方程，適用於 M2 微積分課程。

### 主要功能

#### 函數圖形繪製

- 輸入函數 $f(x)$ 及 $x$ 的範圍（$x_{\min}$、$x_{\max}$）。
- 圖形以 GeoGebra 風格顯示，支援**滾輪縮放**及**拖曳平移**。
- 自動標記所有關鍵特徵點（見下方）。

#### 完整曲線分析（點擊「Analyze」後顯示）

| 分析項目 | 說明 |
|----------|------|
| **定義域** | 函數的定義域 |
| **連續性與可微性** | 判斷函數是否連續及可微 |
| **$f'(x)$（最簡形式）** | 一階導函數，化至最簡 |
| **$f''(x)$（最簡形式）** | 二階導函數，化至最簡 |
| **臨界點** | 極大值、極小值及拐點，清晰標示類型 |
| **單調性** | 函數的遞增 / 遞減區間 |
| **凹凸性** | 函數的上凸 / 下凸區間 |
| **漸近線** | 水平、垂直及斜漸近線（以虛線標示於圖上） |

#### 切線方程

在「Evaluate at $x = $」欄位輸入具體 $x$ 值，計算器輸出：
- $f'(a)$（切線斜率）
- 切線方程 $y = f'(a)(x - a) + f(a)$

#### 逐步微分推導（Step-by-Step Derivation）

輸入函數後，顯示完整的微分步驟，包括：
- 鏈式法則（Chain Rule）
- 積法則（Product Rule）
- 商法則（Quotient Rule）

#### 割線趨向切線視覺化（Secant → Tangent）

調整 $h$ 值（從 1 趨近 0），動態展示割線趨向切線的極限過程，直觀理解導數的幾何意義。

#### AI 微分問題求解器

在「Differentiation Problem Solver」欄位輸入文字題，AI 自動解析並給出完整解答。

### 輸入格式

| 函數類型 | 輸入範例 |
|----------|----------|
| 多項式 | `x^3 - 2*x + 1` |
| 三角函數 | `sin(x)`、`cos(2*x)`、`tan(x)` |
| 指數函數 | `e^x`、`exp(x)` |
| 對數函數 | `ln(x)`、`log(x)` |
| 複合函數 | `sin(x^2)`、`ln(cos(x))` |

---

## 7. 積分：面積與體積 / Integration: Area and Volume

**連結：** https://kingtmc314.github.io/integcalc-lab/

### 功能概覽

本工具計算曲線圍成的面積及旋轉體體積，提供精確符號答案、LaTeX 渲染、互動圖形及動畫 3D 旋轉體，適用於 M2 定積分應用。

### 面積計算

**輸入：**
- $f(x)$（上方曲線）
- $g(x)$（下方曲線，可選）
- 積分下限 $a$ 及上限 $b$（支援表達式，如 `pi/2`、`sqrt(2)`、`ln(3)`）

**輸出：**
- 精確面積值（分數、π、√、e 形式）
- 完整 LaTeX 計算步驟
- 彩色標示圍成區域的互動圖形
- 交點標記

### 體積計算（旋轉體）

本工具自動偵測並選用最適合的計算方法：

| 方法 | 適用情況 | 公式 |
|------|----------|------|
| **碟形法（Disk Method）** | 單一曲線繞 $x$ 軸旋轉 | $V = \pi\int_a^b [f(x)]^2\,dx$ |
| **環形法（Washer Method）** | 兩條曲線繞 $x$ 軸旋轉 | $V = \pi\int_a^b \bigl([f(x)]^2 - [g(x)]^2\bigr)\,dx$ |
| **殼形法（Shell Method）** | 任意曲線繞 $y$ 軸旋轉 | $V = 2\pi\int_a^b x\,f(x)\,dx$ |

**3D 動畫視覺化：** 旋轉體以動畫形式展示，可調整掃描角度（0% → 100%）。

### 預設題目庫

內建 **16 個預設範例**（由基礎至進階），涵蓋：
- 基礎多項式面積
- 三角函數面積
- 旋轉體體積（碟形、環形、殼形）
- 混合曲線圍成區域

### 其他功能

- **複製 LaTeX 代碼**：一鍵複製計算結果的 LaTeX 代碼。
- **互動圖形**：支援滑鼠 / 觸控縮放及平移。
- **精確符號計算**：所有答案以精確值輸出（分數、π、√、e），不使用近似小數。

---

## 8. 不定積分與定積分 / Indefinite & Definite Integration

**連結：** https://kingtmc314.github.io/integral-calculator-students/

### 功能概覽

本工具專為 HKDSE M2 設計，計算方法限於 M2 課程範圍，以 LaTeX 顯示完整逐步解題過程。支援中英文介面。

### 頁面結構

本工具分為多個頁面：

| 頁面 | 功能 |
|------|------|
| **不定積分（主頁）** | 單一不定積分計算 |
| **公式速查（Formulae）** | M2 積分公式參考庫 |
| **多題模式（Multi）** | 一次輸入多道題目，批量計算 |

### 不定積分計算

**輸入格式：**

| 函數類型 | 輸入語法 | 說明 |
|----------|----------|------|
| 乘法 | `x*sin(x)` | 使用 `*` 表示乘號 |
| 次方 | `x^3` | 使用 `^` 表示次方 |
| 自然對數 | `ln(x)` | |
| 常用對數 | `log(x)` | |
| 底數 $b$ 對數 | `log(x,b)` | |
| $\sec^2 x$ | `sec(x)^2` | 注意：`sec(x^2)` 表示 $\sec(x^2)$ |

**支援的積分方法（M2 範圍）：**

- 基本公式（多項式、三角、指數、對數）
- 代換積分法（Substitution）
- 分部積分法（Integration by Parts）
- 三角代換（限 $a\sin\theta$、$a\cos\theta$、$a\tan\theta$ 形式）
- 部分分式分解（Partial Fractions）

**輸出：** 答案以精確值顯示，含積分常數 $C$，並附完整逐步工作步驟。

> **提示：** 若輸入的積分超出 M2 課程範圍，工具會顯示提示訊息，而非強行計算。

### 公式速查頁面

提供 M2 課程所有標準積分公式，以 LaTeX 格式整齊排列，方便複習及參考。

### 多題模式

一次輸入多道積分題目，批量計算並逐一顯示結果，適合練習或批改作業。

---

## 9. 常見問題 FAQ

**Q1：工具顯示的答案是近似值還是精確值？**
所有工具均以精確值輸出（分數、根號、π、e），不使用近似小數，確保答案符合 HKDSE 考試要求。

**Q2：如何切換中英文介面？**
點擊各工具右上角的「中 / EN」語言切換按鈕，設定會自動儲存於瀏覽器。

**Q3：工具可以在手機上使用嗎？**
所有工具均採用響應式設計，支援手機、平板及桌面瀏覽器。建議使用 Chrome 或 Safari 最新版本以獲得最佳體驗。

**Q4：三角計算器需要輸入多少已知量？**
至少需要三個已知量（其中至少一個為邊長）才能唯一求解三角形。若輸入不足，工具會提示缺少資料。

**Q5：線性代數實驗室支援哪些矩陣大小？**
目前支援 2×2 及 3×3 矩陣。特徵值分解及對角化功能適用於 2×2 矩陣（M2 課程範圍）。

**Q6：級數計算器支援哪些通項形式？**
支援多項式通項（$r$、$r^2$、$r^3$）、乘積形式（$r(r+1)$）及部分分式形式（$\frac{1}{r(r+1)}$）。不支援含三角函數或指數函數的通項。

**Q7：積分工具無法計算某些函數怎麼辦？**
若積分超出 M2 課程範圍，工具會顯示提示。此時可嘗試手動化簡後再輸入，或參考公式速查頁面。

---

*本文檔由 Maths Learning Hub 製作，適用於 2025–26 學年 HKDSE 數學課程。*
*This guide is produced by Maths Learning Hub for the 2025–26 HKDSE Mathematics curriculum.*
